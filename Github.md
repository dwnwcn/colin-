# Github



## github基础知识：

*1、介绍：*

世界范围比较有影响力的，项目库托管网站，大量的私人工程或企业级项目在此网站托管发布，里面也包含了很多开源内容，软件研发工程的工具类网站

*2、页面关键部分：*

Topic：分类查看                          搜索栏按关键字查询内容

Trending：推送查看                    按标签查询（sample（一般用来查函数，格式：*关键字+sample*）、tutorial(一般用来查询练习，格式：*关键字+tutorial*)  ）



## 项目结构：

*1、仓库：*

工程存储单位，一般一个仓库中独立存储一个独立项目，一个用户可以有若干个仓库；

* CODE：存储开源数据，开源代码，后续用户下载开源项目，下载的就是code之中的全部内容
* ISSUES：问答板块，解决项目异常、提交bug
* README.md：工程自述文件，进行项目介绍，版本答疑，使用markdown语言编写
* 许可证： 典型的有三个：        GPL3.0       Aphache2.0      MIT   这三个都是给使用者这最小的限制，最大的权力（法务问题）
* 

## git的配置，工程的上传下载：

1、云端仓库：

* 设备认证，生成密钥串，粘贴到github账户中，让设备受信任后续可以完成内容上传
* 创建本地仓库，出现master标志，表示仓库所在位置，默认.git仓库是隐藏文件，使用 git init

2、关于master分支概念：

* 分支：资源存储单位，仓库包含分支，默认情况下仓库都有主分支，默认所有数据都向主分支存储，一个仓库可以有多个分支；、
* 多人协作开发，对分支进行管理以及合并，一系列命令，创建分支，删除分支，分支选择等；

3、相关命令：

命令|作用
:-:|:--
ssh  -T  git@github.com|测试设备关联是否成功
git  config  --list|查看git本地配置文件
git  config  --global user.email "你的邮箱账户"|配置账户
git  config  --global user.name "你的账户名"|配置账户名‘
ssh -keygen  -t  rsa -C "你的邮箱账户"|生成密钥文件，传输加密的方式选择非对称rsa加密（设备指纹）

***注意！！！：密钥配置成功之后才能让本地仓库和云端仓库连接成功***

*方法：生成密钥时记住密钥位置，根据提供的位置，打开.pub密钥文件，复制其中的密钥串，粘贴到指定位置（指定位置在github网站 ->头像 ->Settings ->SSH and GPG key -> New SSH key -> 粘贴密钥  -> add SSH key）*

## 本地数据与云端数据关系

相互依赖，本地为新版，云端为旧版，版本更新使用本地新内容去同步传输给云端



## git 命令实现本地电脑远程反复问修改仓库的数据和内容

过程：开发主机——git缓冲区——host Res（本地仓库）——Github Server——云端

*三部曲：*

1、git add 文件名（上传新文件，没有就添加，有了就修改）

2、git commit -m "说明"（通过命令，将数据通过缓冲区提交给仓库

3、git push 地址（一般是别名） master （将本地的master【主分支】数据推到origin指向的云端仓库中）

***注！！！：在commit步骤之后，一般会先给我们ssh地址起一个别名，方便后续输入地址：git remote add 别名  + 地址（在github网站查看），而git  remote remove +别名则是删除别名***

4、git status:查看缓冲区

​      git rm file：从缓冲区删除

​      git restore file 如果哦本地磁盘删除，可以通过此命令恢复

5、删除逻辑和增添逻辑一致，都是先本地删除，然后同步到云端 



## 下载开源项目

*命令：*git  clone +"工程的https地址"

所有以git仓库为单位的操作都与开发者有关，只是打开下载开源代码和资源文件而已



